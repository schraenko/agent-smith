FROM python:3.14-slim

RUN pip install --upgrade pip
RUN pip install \
    "langchain>=1.0.0" \
    "langchain-ollama>=0.3.0" \
    "langchain-community>=0.3.0" \
    "langchain-core>=0.3.0" \
    httpx pandas ddgs pytest

WORKDIR /workspace
ENTRYPOINT ["python"]
